---
description: Sets up an end-to-end Docker Compose MCP tunnel stack including TLS certificates, proxy config, cloudflared, and a sample server.
disable-model-invocation: false
---

Drive the Docker Compose MCP tunnels quickstart end to end: generate certificates, scaffold the proxy config, start cloudflared, and verify the tunnel stack.
