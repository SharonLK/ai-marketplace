---
description: Adds interactive UI widgets for remote MCP servers and MCPB bundles.
when_to_use: Use when the user wants to add an interactive app UI to their MCP server.
---

# Build MCP App

Adds interactive UI widgets and app interfaces to remote MCP servers and MCPB bundles.
