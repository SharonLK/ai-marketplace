---
description: Entry point for building MCP servers — routes to the appropriate deployment model (remote HTTP, MCPB, or local stdio).
when_to_use: Triggered when the user asks to build an MCP server or says "help me build an MCP server".
---

# Build MCP Server

Routes to the right deployment model: remote HTTP, MCPB bundle, or local stdio.
