---
description: Explains the canonical Claude Code plugin directory structure and plugin.json schema requirements.
when_to_use: Triggered when the user wants to understand how to lay out a plugin, what fields plugin.json requires, or how content files should be organized.
---

# Plugin Structure

Covers plugin layout conventions: the .claude-plugin/ directory, plugin.json schema fields (name, displayName, version, description, author, and type-specific keys), and content file organization.
