---
description: Guides adding user-configurable settings to a Claude Code plugin via settings schema definitions.
when_to_use: Triggered when the user wants to add configurable options to their plugin or understand how plugin settings work.
---

# Plugin Settings

Guides plugin settings: defining a settings schema in plugin.json, providing defaults, and reading settings values at runtime in hooks and skills.
